$('#task_list .panel[data-id=<%= @task.id%>]').replaceWith("<%= j render partial: 'task', locals: {task: @task } %>")
$('#task_list .panel[data-id=<%= @task.id%>]').effect('pulsate');
$('#task_form_modal').modal_success();
$(document).trigger('tasks:change');
