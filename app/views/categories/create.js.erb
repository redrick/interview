$('#category_list').prepend("<%= j render partial: 'category', locals: {category: @category } %>");
$('#category_list .panel[data-id=<%= @category.id%>]').effect('pulsate');
$('#category_form_modal').modal_success();
