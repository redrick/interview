$('#user_list .panel[data-id=<%= @user.id%>]').replaceWith("<%= j render partial: 'user', locals: {user: @user } %>")
$('#user_list .panel[data-id=<%= @user.id%>]').effect('pulsate');
$('#user_form_modal').modal_success();
